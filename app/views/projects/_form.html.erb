<%= form_with(model: project) do |form| %>
  <% if project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class = "text", id = "projectdiv">
    <div>
      <%= form.label :project_title, style: "display: block" %>
      <%= form.text_field :project_title, class: "form-control"  %>
    </div>

    <div>
      <%= form.label :project_description, style: "display: block" %>
      <%= form.text_area :project_description, class: "form-control" %>
    </div>
  </div>

  <div>

    <%= form.submit class: "btn btn-dark" %>

  </div>
<% end %>
